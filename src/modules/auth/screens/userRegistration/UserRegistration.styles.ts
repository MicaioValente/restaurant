import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  image: {
    alignSelf: 'center',
    marginHorizontal: 50,
  },
  formControl: {
    marginBottom: 18,
  },
  containerButton: {
    marginTop: 'auto',
    marginBottom: 50,
  },
});
